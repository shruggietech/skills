# v1.7.2 - 2026-07-06

## Fixed

- `shruggie-graph-memory`: `assets/capture-reference.md` now states that a `context` which is
  malformed or names a memory the connection is not linked to is refused (bad input or denied),
  never silently written to or searched in the default memory. Reconciles the reference with the
  ShruggieGraph S070 fail-closed change; the `create_note` `context` note and the Memory targeting
  section are updated.
