# v1.7.0 - 2026-07-05

## Fixed

- `shruggie-graph-memory`: rewritten to the live ShruggieGraph tool surface. The shipped skill
  still instructed clients to pass a required `workspace_id` to `create_note` and `workspace_ids`
  to `search_knowledge`, parameters the server removed when workspaces were collapsed into
  tenants; calls following the skill failed. `SKILL.md`, `README.md`, and
  `assets/capture-reference.md` now document token-linked memory targeting (the connection
  determines where notes land; an optional `context` string selects among several linked
  memories), both connection paths (the claude.ai OAuth connector at
  `https://graph.shruggie.tech/mcp` and the Claude Desktop `mcp-stdio` adapter), and the current
  console vocabulary (Connected apps page; no scope selector, no workspace links). The
  normal-sensitivity cap and scope guidance are unchanged
